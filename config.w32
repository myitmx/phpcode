// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("decode", "for decode support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("decode", "enable decode support", "no");

if (PHP_DECODE != "no") {
	EXTENSION("decode", "decode.c");
}

